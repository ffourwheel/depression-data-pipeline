# 🧠 AIE321 Mini Project: Depression Data Pipeline
# Depression Data Pipeline Project (ระบบจัดการข้อมูลภาวะซึมเศร้า)

## 📌 ภาพรวมโปรเจกต์ (Project Overview)
โปรเจกต์นี้จำลองระบบ **Automated Data Engineering Pipeline** เพื่อจัดการข้อมูลประชากรและสุขภาพจิต (Depression Data) โดยระบบจะทำการดึงข้อมูลดิบ (Raw CSV), แปลงและวิเคราะห์ข้อมูล (Transform), และส่งผลลัพธ์ขึ้นสู่ Google Sheets เพื่อทำรายงานโดยอัตโนมัติ

**จุดเด่นของระบบ:**
- **Automated Pipeline:** ทำงานอัตโนมัติ 100% ผ่าน Docker Compose
- **High Performance:** ใช้เทคนิค `COPY` Command ของ PostgreSQL ในการโหลดข้อมูล ทำให้รองรับข้อมูลขนาดใหญ่ (400,000+ แถว) ได้ในไม่กี่วินาที (Turbo Mode)
- **Scalability:** มีระบบจัดการ Memory และแบ่งส่งข้อมูล (Batch Processing) ทำให้เครื่องไม่ค้างแม้ข้อมูลจะเยอะ
- **Reporting:** ส่งข้อมูลเข้า Google Sheets อัตโนมัติ พร้อมระบบป้องกัน API Limit

---

## 🏗 สถาปัตยกรรมระบบ (Pipeline Architecture)

ระบบประกอบด้วย 3 ขั้นตอนหลัก ซึ่งถูกสั่งการผ่าน `run_pipeline.py`:

### 1. Ingest (`ingest.py`)
- อ่านไฟล์ข้อมูลดิบ `depression_data.csv`
- ใช้เทคนิคขั้นสูง **PostgreSQL COPY Command** แทนการ Insert ปกติ
- **ผลลัพธ์:** ข้อมูล 400,000+ แถวถูกนำเข้าฐานข้อมูล (`raw_data`) ทันที

### 2. Transform (`transform.py`)
- ดึงข้อมูลจากฐานข้อมูลมาทำความสะอาด (Data Cleaning)
  - แปลงชื่อคอลัมน์ให้เป็นมาตรฐาน (snake_case)
  - จัดการค่าว่าง (Null/NaN) และแปลงประเภทตัวแปร
- **Feature Engineering (วิเคราะห์ข้อมูลเพิ่ม):**
  - จัดกลุ่มช่วงอายุ (`age_group`)
  - คำนวณค่าสถิติกลุ่ม (Aggregation) เช่น รายได้เฉลี่ยของคนรุ่นเดียวกัน
  - นำค่าสถิติแปะกลับเข้าไปในข้อมูลดิบ (Merge) เพื่อให้เห็นข้อมูลครบทุกมิติ
- บันทึกลงตาราง `clean_data` ด้วยโหมดความเร็วสูง (Turbo Mode)

### 3. Publish (`publish.py`)
- ดึงข้อมูลที่ผ่านการวิเคราะห์แล้ว
- จัดการค่า Error ทางคณิตศาสตร์ (NaN, Infinity) เพื่อให้ Google Sheets รับค่าได้
- **Batch Upload:** ทยอยอัปโหลดข้อมูลทีละส่วน (Chunking) เพื่อป้องกันไม่ให้ Google API ตัดการเชื่อมต่อและประหยัด RAM

---

## 📊 พจนานุกรมข้อมูล (Data Dictionary)

มีการเพิ่มคอลัมน์ใหม่จากการวิเคราะห์ข้อมูล (Transform Stage) ดังนี้:

| ชื่อคอลัมน์ | คำอธิบาย | ประโยชน์ในการใช้งาน |
| :--- | :--- | :--- |
| **`age_group`** | กลุ่มช่วงอายุ (เช่น Teen, Senior) | ช่วยให้วิเคราะห์ข้อมูลตามเจเนอเรชันได้ง่ายกว่าตัวเลขอายุโดดๆ |
| **`group_total_records`** | จำนวนคนทั้งหมดในกลุ่มเดียวกัน | **ความน่าเชื่อถือ:** บอกว่ากลุ่มนี้มีประชากรเยอะแค่ไหน ข้อมูลกลุ่มนี้เชื่อถือได้หรือไม่ |
| **`group_avg_income`** | รายได้เฉลี่ยของคนในกลุ่มเดียวกัน | **เกณฑ์เปรียบเทียบ:** ใช้ดูว่ารายได้ของคนคนนี้ "มาก" หรือ "น้อย" เมื่อเทียบกับค่าเฉลี่ยของคนรุ่นเดียวกัน |

---

## 🚀 วิธีการรัน (How to Run)

1. **เตรียมความพร้อม:**
   - ติดตั้ง Docker และ Docker Compose
   - วางไฟล์ `service-account-key.json` (Google API Key) ในโฟลเดอร์ `/files`
   - วางไฟล์ข้อมูลดิบ `depression_data.csv` ในโฟลเดอร์ `/files`

2. **สั่งรันระบบ:**
   เปิด Terminal แล้วพิมพ์คำสั่ง:
   ```bash
   docker compose up --build

## 📂 โครงสร้างไฟล์ (File Structure)

```text
/app
  |- files/                   # เก็บไฟล์ข้อมูลและไฟล์ความลับ (Volume)
  |   |- depression_data.csv      # ข้อมูลดิบ (Source Data)
  |   |- service-account-key.json # (ห้าม Commit) Key สำหรับ Google Sheets
  |- ingest.py                # Script: โหลด CSV เข้า DB
  |- transform.py             # Script: Clean, Grouping และ Aggregate ข้อมูล
  |- publish.py               # Script: อัปโหลดข้อมูลสรุปขึ้น Google Sheets
  |- run_pipeline.py          # Main Script: รันทั้ง 3 ขั้นตอน
  |- requirements.txt         # Python dependencies
  |- Dockerfile               # Docker configuration
  |- docker-compose.yml       # Docker Compose configuration
  |- .gitignore               # Git ignore rules
  |- README.md                # Project documentation